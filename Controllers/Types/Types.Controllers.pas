unit Types.Controllers;

interface

type
  TTypeOperation = (toInsert, toUpdate, toShow, toDelete, toDuplicate);
  TTypeStatus = (tsActive, tsInactive);

  TTypePersons = (tpClient, tpProvider);

implementation

end.
